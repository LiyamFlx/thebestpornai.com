{
  "name": "streamhub-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "node deploy.js",
    "deploy:apply": "node deploy.js --apply",
    "deploy:build": "npm run build && node deploy.js --apply"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
