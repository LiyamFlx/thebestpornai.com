# Video media — exclude private/source videos, but allow royalty-free demo samples
media/*.mp4
media/*.mov
media/*.webm
media/*.mkv
!media/sample-*.mp4

# Generated poster thumbnails (gen-posters.js) — uploaded to R2, never committed
media/thumbs/
media/**/*.jpg
media/**/*.jpeg

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Build / dev artifacts
.playwright-mcp/
*.zip
dist/
node_modules/

# Local bulk-upload staging — videos live on Bunny CDN, not in repo
media/new balk uplode/
media/**/*.mp4
media/**/*.mov
media/**/*.webm
media/**/*.mkv
.vercel

# Brainstorming visual-companion sessions (ephemeral mockups, not source)
.superpowers/

# Environment secrets — never commit
.env
.env.*
!.env.example

# Staging / temporary manifest and video catalog helper files
missing-videos.json
manifest.json
src/shared/catalog.js.bak
